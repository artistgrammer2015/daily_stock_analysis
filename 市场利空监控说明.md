# 个股利空消息监控功能

## 功能介绍

自动监控 **具体个股** 的重大利空事件，并通过 Telegram 实时推送。

**重点**：监控的是**个股级别的利空**（如 UNH 被联邦调查、闻泰科技被接管），而不是大盘整体消息。

## 监控范围

### 监管与法律
- 证监会/SEC 立案调查
- 公司被处罚、罚款
- 董事长/高管被查、逮捕
- 重大诉讼、法律纠纷

### 财务问题
- 业绩暴雷、预亏公告
- 财务造假、审计问题
- 债务违约、破产风险

### 重大变故
- 公司被接管、重组
- ST/退市警示
- 高管辞职、跑路
- 资产冻结、停牌

### 典型案例
- ✅ "UNH 被联邦调查" - 具体公司事件
- ✅ "闻泰科技被荷兰接管" - 具体公司事件
- ✅ "康美药业财务造假被处罚" - 具体公司事件
- ❌ "A股大盘暴跌" - 大盘消息（会被过滤）
- ❌ "科技板块下跌" - 板块消息（会被过滤）

## 配置方法

### 1. 安装依赖

**使用 Tavily (推荐)**
```bash
pip install tavily-python
```

**或使用 SerpAPI**
```bash
pip install google-search-results
```

### 2. 配置 API Keys

在 `.env` 文件中添加：

```env
# Tavily API Keys（逗号分隔，支持多个）
TAVILY_API_KEYS=your_tavily_key_1,your_tavily_key_2

# 或者使用 SerpAPI Keys
SERPAPI_API_KEYS=your_serpapi_key_1,your_serpapi_key_2
```

#### 获取 API Keys

- **Tavily**: https://tavily.com/ （每月免费 1000 次搜索）
- **SerpAPI**: https://serpapi.com/ （每月免费 100 次搜索）

### 3. 启用功能

利空消息监控已默认集成到 `main_scan()` 流程中，运行时自动执行：

```bash
python a_stocks.py
```

### 4. 独立运行（可选）

如果只想运行利空监控而不执行其他任务，可以在 Python 中直接调用：

```python
from a_stocks import monitor_stock_negative_news

monitor_stock_negative_news()
```

## 消息示例

```
⚠️ 个股利空消息监控 (2026-01-30)
共发现 5 条重大利空事件

1. [UnitedHealth (UNH) faces federal investigation over claims practices](https://example.com/news1)
🕐 2026-01-30 09:30
💬 Federal authorities have launched an *investigation* into UnitedHealth Group's claims denial practices, focusing on potential violations of healthcare regulations...

2. [闻泰科技被荷兰政府接管，面临资产重组](https://example.com/news2)
🕐 2026-01-30 10:15
💬 闻泰科技旗下欧洲子公司因财务问题被荷兰政府*接管*，公司将面临大规模资产重组...

3. [康美药业因财务造假被证监会重罚300亿元](https://example.com/news3)
🕐 2026-01-29 16:20
💬 证监会今日对康美药业作出行政*处罚*决定，认定其存在重大财务造假行为...

4. [某生物科技公司CEO因欺诈指控被FBI逮捕](https://example.com/news4)
🕐 2026-01-29 14:10
💬 FBI announced the arrest of biotech company CEO on charges of securities *fraud*...
```

## 搜索策略

- **时间范围**: Tavily 搜索最近 3 天，SerpAPI 搜索最近 1 周
- **搜索关键词**: 6 个精准的个股利空关键词
- **结果数量**: 每个关键词最多 5 条结果
- **智能过滤**: 自动过滤大盘/板块新闻，只保留个股事件
- **去重机制**: 根据标题自动去重
- **限流保护**: 每次搜索间隔 2 秒
- **关键词高亮**: 自动高亮"调查"、"处罚"、"暴雷"等关键词

## 自定义搜索关键词

编辑 `a_stocks.py` 中的 `monitor_stock_negative_news()` 函数，修改 `search_queries` 列表：

```python
search_queries = [
    # 监管和法律
    "上市公司 证监会 立案调查",
    "上市公司 SEC 调查",

    # 财务问题
    "公司 业绩暴雷 预亏",
    "公司 财务造假 退市",

    # 添加自定义关键词
    "科技公司 数据泄露 罚款",
    "药企 临床试验 失败",
    # ... 更多关键词
]
```

## 注意事项

1. **API 配额**: Tavily 和 SerpAPI 都有免费配额限制，建议配置多个 Key 轮询使用
2. **搜索频率**: 默认每次运行搜索一次，避免频繁调用浪费配额
3. **误报过滤**: 可以根据实际情况调整搜索关键词，减少误报
4. **网络要求**: 需要能访问 Tavily/SerpAPI 服务

## 定时任务

如果需要定时监控，可以在 crontab 中添加：

```bash
# 每天 9:30 监控一次
30 9 * * 1-5 /usr/bin/python3 /path/to/a_stocks.py >> /path/to/a_stock.log 2>&1
```

## 故障排查

### 1. 提示"未配置 Tavily 或 SerpAPI Keys"

检查 `.env` 文件是否正确配置了 API Keys。

### 2. 提示"未安装 tavily-python"

运行 `pip install tavily-python` 安装依赖。

### 3. 搜索结果为空

- 检查搜索关键词是否合适
- 尝试使用更通用的关键词
- 检查 API Key 是否有效

### 4. API 限流错误

- 配置多个 API Keys 进行负载均衡
- 减少搜索关键词数量
- 增加搜索间隔时间

## 进阶配置

### 添加更多过滤规则

在代码中添加自定义过滤规则，排除不想看到的内容：

```python
# 在 monitor_stock_negative_news() 函数中添加
# 过滤掉广告、软文等
excluded_keywords = ['广告', '推广', 'AD', '赞助']
if any(keyword in title for keyword in excluded_keywords):
    continue
```

### 调整时间范围

修改搜索参数：

```python
# Tavily
response = client.search(
    query=query,
    days=7,  # 改为搜索最近 7 天
)

# SerpAPI
params = {
    "tbs": "qdr:m",  # 改为搜索最近 1 个月
}
```

### 监控特定公司

添加针对特定公司的关键词：

```python
search_queries = [
    # 通用关键词
    "上市公司 证监会 立案调查",

    # 特定公司监控
    "特斯拉 Tesla 诉讼 调查",
    "腾讯 Tencent 反垄断",
    "比亚迪 BYD 召回",
]
```

## 技术支持

如有问题，请查看日志文件：`logs/a_stock.log`
